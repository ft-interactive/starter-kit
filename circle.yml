machine:
  node:
    version: 6.3.0

dependencies:
  cache_directories:
    - bower_components
    - node_modules
  override:
    - npm install

notify:
  webhooks:
    # Notifies GHPM of build
    - url: https://ft-ig-github-project-manager.herokuapp.com/build
